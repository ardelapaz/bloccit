<h1>New Advertisement</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for ad</p>
     <ul>
       <li>Double check to make sure it rhymes.</li>
       <li>Has to be related to Kayne West in some way.</li>
       <li>If you think Whataburger is better than Five Guys just leave now.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @advertisements do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter ad title" %>
       </div>
       <div class="form-group">
         <%= f.label :copy %>
         <%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter ad body" %>
       </div>
       <div class="form-group">
         <%= f.label :price %>
         <%= f.text_area :price, rows: 8, class: 'form-control', placeholder: "Enter price" %>
       </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>